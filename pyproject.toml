[project]
name = "sea"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.100.2",
    "pydantic>=2.11.7",
]

[dependency-groups]
dev = [
    "ruff>=0.12.9",
]
